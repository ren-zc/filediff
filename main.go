package main

import (
	// "fmt"
	"github.com/jacenr/filediff/diff"
)

func main() {
	diff.Diff()
	// var srcFile = "SrcFile"
	// var dstFile = "DstFile"
	// a := diff.ReadFile(srcFile)
	// fmt.Println(a)
	// graph := diff.InitGraph()
	// // fmt.Println()
	// for i, val := range graph {
	// 	for j, val2 := range val {
	// 		// fmt.Println(val2)
	// 		fmt.Printf("%d,%d\t%v\n", i, j, val2)
	// 	}
	// }
}
